{% extends 'layout.njk' %}

{% block head %}
    <title>Receitas</title>
{% endblock %}

{% block content %}
    <main>
        <section id="recipes">
            <div id="cards">

                {% for recipe in recipes %}
                    <div class="card">
                        <img src="{{ recipe.image }}" alt="Imagem de {{ recipe.title }}">
                        <div class="info">
                            <h3>{{ recipe.title }}</h3>
                            <p class="author">por
                                {{ recipe.author }}</p>
                        </div>
                    </div>
                {% endfor %}

            </div>
        </section>
    </main>
{% endblock %}